1. if input element is bigger than 9, then it will be convert to elements smaller than 9, e.g. if input is {23}, then will be converted to {2,3} first.

2. there are two projects: parent and core project, core project depend on parent project

3. use JDK1.8 which is configged at settings.xml